Reflection - Project 3 - Octopus - Kaylin Francisco

For this assignment I decided to work from top to bottom of the screenshot page. First I linked the css page and started adjusting the header by using some of the aligning techniques we used in pre-classes and labs. I decided to add an inline style element on line 15 to change the text to be italic. Below the header, adjusting the paragraph and lists section was pretty easy, I learned about text-transform, which was used to make the text uppercase. There were some points in the index file where I added a class in order to change some styles. One example was for the "mantle" text. Also for the octopus image and the quiz form. I kept the order of everthing the same. For me the most difficult part was the Octopus quiz form. Just adjusting the image on the right and keeping the text on the left was a little difficult to do, but I just needed to use "float: right" on the image. I also got stumped when adjusting the padding around the species table data. Shortly after I realized you can do "#species-table td, th" in order to change that information. Overall this assignment wasn't too bad to figure out, and it gave me a chance to play around with a few different CSS properties that are useful for web development.